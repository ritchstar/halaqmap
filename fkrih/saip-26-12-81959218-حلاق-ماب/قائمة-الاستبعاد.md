# قائمة الاستبعاد — أرشيف طلب SAIP `26-12-81959218`

هذا التعليق يرافق الأرشيف الكامل للكود المصدري لمنصة حلاق ماب. الفاحص طلب **كامل الأكواد البرمجية**. لذلك **لم يُستبعد أي ملف مصدري** من المستودع المتتبَّع بسبب «حساسية التعبير البرمجي» أو «أجزاء تمثيلية».

ما يلي **ليس كوداً مصدرياً لهذا المصنف**، أو أسراراً تشغيلية، أو مواد تثير شبهة مالية أو خلطاً بمصنفات أخرى.

| المستبعد | السبب |
|----------|--------|
| `node_modules/` | اعتمادات طرف ثالث تُسترجع بـ `npm install` من `package.json` / `package-lock.json` الموجودين داخل الأرشيف. ليست تعبيراً أصلياً للمؤلف. |
| `.env` و`.env.*` عدا الأمثلة | أسرار تشغيل (مفاتيح ميسر، `SUPABASE_SERVICE_ROLE_KEY`، وغيرها). الكود يقرأها من البيئة؛ الأمثلة `.env.example` داخل الأرشيف. |
| مفاتيح API الفعلية غير الموجودة في Git | غير مودعة في المستودع أصلاً. |
| `dist/`، `.vite/`، `.vercel/`، `coverage/`، `.next/` | مخرجات بناء مؤقتة تُعاد توليدها من المصدر. |
| `ios/App/Pods/`، `android-partner-twa/.gradle/`، ملفات `.keystore` / `.jks` | بناء أصلي ومفاتيح توقيع؛ ليست نص المصنف. |
| `BANK_DETAILS.txt` | تعليمات تحويل تشغيلي فيها رقم آيبان. ليست كوداً مصدرياً؛ حُذفت من الشجرة العاملة واستُبعدت من المرفق حتى يبقى الطلب خالياً من شبهة بيانات بنكية. |
| `START_HERE.txt` | دليل تسليم تشغيلي كان يكرر الآيبان. ليس كود المنصة؛ يُستبعد من المرفق. |
| `DASHBOARD_ACCESS.txt` | ملاحظات دخول تجريبية. ليست كوداً مصدرياً؛ حُذفت واستُبعدت. |
| ملفات حصر `fkrih/` لغير هذا الطلب | مصنفات مستقلة (افراحي1 وغيرها). موضوع هذا المرفق منصة حلاق ماب فقط. يبقى داخل المرفق: `fkrih/حلاق-ماب.md` و`fkrih/بيان-المؤلف.md` ومجلد هذا الطلب. |
| `Twilio/` (غير متتبَّع) | معرفات حساب محلية؛ ليست جزءاً من Git ولا من المرفق. |
| `google-ads/`، `youtube-channel/`، `moyasar/` (غير متتبَّعة) | أصول تسويق ومجموعة بوستمان محلية بآيبانات أمثلة من وثائق ميسر لا تخص مقدم الطلب. ليست مصدر المنصة المتتبَّع. |
| `halaqmap_partners/`، `halaqmap7/`، `nazamsafir/` (غير متتبَّعة) | نسخ أو مواد خارج شجرة المصدر الرسمية. |
| `supabase/.temp/` | كاش CLI محلي. |
| `uploaded_files/` | رفعات تشغيل لا مصدر. |

**ما بقي داخل الأرشيف:** الملفات المتتبَّعة لشجرة المنصة (واجهة `src/`، دوال `api/`، مهاجرات `supabase/`، `public/`، `scripts/`، إعدادات البناء، `package.json` باسم المؤلف `BINSARRA AHMED ABDULLAH A`) مع ملفَّي حصر هذا الطلب. لم تُحذف وحدات المتجر اللاحقة من شجرة المصدر لأنها في نفس المستودع؛ حذفها يجعل أكواد المنصة ناقصة أمام طلب «كامل الأكواد». موضوع التسجيل يبقى منصة حلاق ماب فقط (`26-12-81959218`).

المولّد يرفض إصدار PDF إذا وُجد رقم آيبان سعودي مكتمل في أي ملف نصي مُدرَج.

**ملف الرفع:** `halaqmap-saip-26-12-81959218-source.pdf` — فهرس المسارات ثم محتوى كل ملف نصي بالتسلسل، والملفات الثنائية (صور وخطوط) مُدرجة بالمسار والحجم و`SHA-256` دون تضمين نقطي. حد البوابة 1024 ميغابايت.

**ZIP المحلي** يبقى نسخة عمل فقط ولا يُرفع في حقل إرفاق المصنف.

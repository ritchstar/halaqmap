import { PackageCheck } from "lucide-react";import { cn } from "@/lib/utils";
type TamwinataMarkProps = { className?: string; size?: "sm" | "md" | "lg"; inverse?: boolean;};
const sizeClasses = { sm: "size-9 rounded-lg", md: "size-11 rounded-xl", lg: "size-14 rounded-2xl",};
export function TamwinataMark({ className, size = "md", inverse = false,}: TamwinataMarkProps) { const iconSize = size === "sm" ? 17 : size === "md" ? 21 : 27;
 return (    <span      aria-label="دمغة تمويناتا1"      className={cn(        "relative inline-flex shrink-0 items-center justify-center overflow-hidden border shadow-sm",        sizeClasses[size],        inverse          ? "border-white/20 bg-white/10 text-orange-300"          : "border-orange-200 bg-orange-50 text-orange-600",        className,      )}    >      <span className="absolute inset-x-1.5 bottom-1.5 h-1 rounded-full bg-current opacity-25" />      <PackageCheck size={iconSize} strokeWidth={2} className="relative" />    </span> );}